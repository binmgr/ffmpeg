# Claude instructions

Read `IDEA.md` for project intent and variables.
Read `SPEC.md` for project-specific rule overrides (these win over global conventions).

No `AI.md` — this is a CI/distribution project with no language template (see SPEC.md Exception 4).
